services:
  postgres:
    image: postgres:18-alpine
    volumes:
        - postgres:/var/lib/postgresql/data
    env_file:
      - .env
    ports:
      - "127.0.0.1:5432:5432"

volumes:
  postgres:
    name: arg-invoice-app-postgres
